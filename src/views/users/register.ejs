<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="/image/Logos/Logophone.png" />
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@100&display=swap" rel="stylesheet">
    <link rel="stylesheet" href=" https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.11.2/css/all.min.css ">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-eOJMYsd53ii+scO/bJGFsiCZc+5NDVN2yr8+0RDqr0Ql0h+rP48ckxlpbzKgwra6" crossorigin="anonymous">
    <link rel="stylesheet" href="../../css/estilosLogin.css">
    <title>JBike</title>

</head>

<body>
    <div class="container-login">
        <div class="background-login">
            <img src = "../image/login/registerimage3.jpeg" width="100%" height = "100%" >
        </div>
        <div class="form-login">
            <div class="form-container">

            
                <a href="/">
                    <img src="../image/Logos/Logophone.png" width = 100px>
                </a>
                <div class="ingreso">
    
                    <h3>Crear una cuenta</h3>
                    <form action="/users/create" method="POST"  class = "formRegister">


                        <div class = "inputs">

                        <% if (locals.errores && errores.firstname) { %>
                        <p class="errorFront"> <%= errores.firstname.msg  %> ! </p>
                        <% } %>
                        <p id = "nombreError" class = "errorFront"></p>
                        <input type="text" name="firstname" placeholder="Nombre" value ="<%= locals.oldData ? oldData.firstname : null  %>" id="nombreUsuario" class ="input" >
                       
                        <% if (locals.errores && errores.lastname) { %>
                            <p class="errorFront"> <%= errores.lastname.msg  %> ! </p>
                        <% } %>
                        <p id = "apellidoError" class = "errorFront"></p>
                        <input type="text" name="lastname" placeholder="Apellido" value ="<%= locals.oldData ? oldData.lastname : null  %>" id="apellidoUsuario" class ="input" >
                       
                        <% if (locals.errores && errores.phone) { %>
                            <p class="errorFront"> <%= errores.phone.msg  %> ! </p>
                            <% } %>
                        <p id = "phoneError" class = "errorFront"></p>
                        <input type="text" name = "phone" placeholder="Numero de telefono" value ="<%= locals.oldData ? oldData.phone : null %>" id = "telefono">
                       

                        <% if (locals.errores && errores.email) { %>
                            <p class="errorFront"> <%= errores.email.msg  %> ! </p>
                        <% } %>
                        <p id = "emailError" class = "errorFront"></p>
                        <input type="email" name="email" placeholder="Correo electronico" value ="<%= locals.oldData ? oldData.email : null %>" class="input" id = "email"  >
                      

                        <% if (locals.errores && errores.password) { %>
                            <p class="errorFront"> <%= errores.password.msg  %> ! </p>
                        <% } %>   
                        <p id = "passError" class = "errorFront"></p>
                        <input type="password" name="password" placeholder="Contraseña" class="input" id = "password"> 
 
                        <p id = "confrimpassError" class = "errorFront"></p>
                        <input type="password" name = "confirmarPass" placeholder="Confirme la contaseña" class = "input" id ="passConfirm">
                        

                        <% if (locals.errores && errores.genre) { %>
                            <p class="errorFront"> <%= errores.genre.msg  %> ! </p>
                        <% } %>   
                        <select name="genre" class="input" style = "margin-top: 8px; ">
                            <option value ="" name  = "vacio">Seleccione el genero</option>
                            <% for(const oneGenre of genres ) { %>
                                <option value = "<%= oneGenre.id %>" <%= locals.oldData && (oneGenre.id == oldData.genre) ? 'selected' :null %> name = "genre"> <%= oneGenre.name %>  </option>
                            <% } %>
                        </select>

                        <br>

                        <% if (locals.errores && errores.rol) { %>
                            <p class="errorFront"> <%= errores.rol.msg  %> ! </p>
                        <% } %>   
                        <select name="rol" class="input" style = "margin-top: 20px; margin-bottom: -10px;">
                            <option value ="" name  = "vacio">Seleccione el Rol</option>
                            <% for(const oneRol of roles) { %>
                                <option value = "<%= oneRol.id %>" <%= locals.oldData && (oneRol.id == oldData.rol) ? 'selected' :null %> name = "rol" > <%= oneRol.name %>  </option>
                            <% } %>
                        </select>




                    </div>
                    <br>
  

                
                    <div class="buttons">
                        <a href="/index"></a>   
                            <button type = "submit" class="login-botton">
                                Ingresar
                            </button>
                     
                    </div>
                    Ya tenes una cuenta? <a href="/users/login">Ingresa aca</a>
                </form>
                </div>
        </div>
    </div>
    </div>
    <script src = "/js/validacion-register.js"></script>


</body>


</html>